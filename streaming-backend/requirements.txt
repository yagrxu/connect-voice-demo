fastapi>=0.110
uvicorn[standard]>=0.29
websockets>=12
boto3>=1.34
amazon-transcribe>=0.6.2
webrtcvad-wheels>=2.0.14  # prebuilt wheels (module still imports as `webrtcvad`); avoids compiling in slim image
# Nova Sonic (aws-sdk-bedrock-runtime) needs awscrt~=0.32 which CONFLICTS with
# amazon-transcribe's awscrt~=0.26 — it runs in a SEPARATE service/image.
